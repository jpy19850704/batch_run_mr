apiVersion: v1
kind: Service
metadata:
  name: mr-app
  labels:
    app: mr-app
spec:
  type: ClusterIP
  selector:
    app: mr-app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP

