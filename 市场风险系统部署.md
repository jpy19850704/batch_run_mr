# 市场风险系统部署文档

## 一、项目结构

```
zcyh_mr/
├── engine/                              # Spring Boot 主工程（仅保留代码与构建资产）
│   ├── pom.xml                          # 父 POM（多模块）
│   ├── mr-core/                         # 核心模块（计算引擎）
│   │   └── pom.xml
│   ├── mr-app/                          # 应用模块（Web 服务）
│   │   ├── pom.xml
│   │   └── src/main/
│   │       ├── java/com/zcyh/mr/springboot/  # Spring Boot 启动与 API 外壳
│   │       └── resources/                    # Spring Boot 资源与配置
│   └── src/main/
│       └── java/com/zcyh/mr/            # 业务代码（Git 子仓）
│           ├── calc/                    # 计算引擎
│           ├── loader/                  # 数据加载与校验
│           ├── marketdata/              # 市场数据模型
│           ├── product/                 # 产品估值模型
│           ├── frtbsa/                  # FRTB 标准法
│           ├── scenario/                # 情景分析
│           └── outer/engine/            # 外部引擎适配器
│
├── docker/                              # 独立 Docker 目录（与 engine 脱离）
│   ├── Dockerfile                       # Docker 镜像构建文件
│   ├── docker-compose.yml               # 本地容器联调
│   ├── build-image.ps1                  # Windows 构建脚本
│   └── build-image.sh                   # Linux 构建脚本
│
└── k8s/                                 # 独立部署目录（与 engine 脱离）
    ├── base/                            # 基础资源清单
    ├── overlays/                        # 环境差异化配置
    └── scripts/                         # K8s 安装与部署脚本
```

### 技术栈

| 组件 | 版本 |
|------|------|
| JDK | 17 (LTS) |
| Spring Boot | 2.7.18 |
| 构建工具 | Maven 多模块 |
| 容器化 | Docker（Jar 产物构建运行镜像） |
| 运行时镜像 | eclipse-temurin:17-jre-alpine |
| K8s | k3s（轻量）或标准 kubeadm |

---

## 二、Docker 镜像构建

### 2.1 Dockerfile 说明

文件位置：`docker/Dockerfile`

```dockerfile
FROM eclipse-temurin:17-jre-alpine
WORKDIR /opt/mr-app

# 时区与字符集
ENV TZ=Asia/Shanghai LANG=C.UTF-8
RUN apk add --no-cache tzdata

# 非 root 用户运行
RUN addgroup -S mrapp && adduser -S mrapp -G mrapp
COPY build/app.jar /opt/mr-app/app.jar
RUN chown -R mrapp:mrapp /opt/mr-app
USER mrapp

EXPOSE 8080

# JVM 容器感知参数
ENV JAVA_OPTS="-XX:MaxRAMPercentage=75.0 \
    -XX:InitialRAMPercentage=50.0 \
    -XX:+UseG1GC \
    -XX:+UseStringDeduplication \
    -Djava.security.egd=file:/dev/./urandom"

ENTRYPOINT ["sh", "-c", "java $JAVA_OPTS -jar /opt/mr-app/app.jar"]

HEALTHCHECK --interval=30s --timeout=3s --start-period=60s --retries=3 \
    CMD wget -qO- http://localhost:8080/healthz || exit 1
```

### 2.2 构建模式

```
第 1 步：在 engine/ 中执行 Maven package，生成 mr-app fat jar
第 2 步：将生成的 jar 复制到 docker/build/app.jar
第 3 步：Docker 仅基于 jar 构建运行镜像
```

### 2.3 构建命令

```bash
# 在 zcyh_mr 根目录下执行
cd zcyh_mr/

# 先打包
mvn -f engine/pom.xml -pl mr-app -am -DskipTests package

# 复制产物
cp engine/mr-app/target/mr-app-1.0-SNAPSHOT.jar docker/build/app.jar

# 构建镜像
docker build -t mr-app:latest -f docker/Dockerfile docker/

# 验证镜像
docker images mr-app

# 本地运行测试
docker run -d --name mr-test -p 8080:8080 mr-app:latest

# 查看日志
docker logs -f mr-test

# 访问健康检查
curl http://localhost:8080/healthz

# 清理
docker stop mr-test && docker rm mr-test
```

### 2.4 推送到镜像仓库

```bash
# 标记镜像
docker tag mr-app:latest your-registry.com/zcyh/mr-app:v1.0.0

# 登录仓库
docker login your-registry.com

# 推送
docker push your-registry.com/zcyh/mr-app:v1.0.0
```

### 2.5 设计说明

- Docker 目录独立于 engine，便于后续部署资产单独维护。
- Docker 构建基于 Jar 产物，不再在镜像构建过程中编译源码。
- 系统程序只有一个 Spring Boot 应用，不拆分 `mr-api` 和 `mr-worker` 两套程序。
- 同一镜像既可承担 API 接入职责，也可承担后台执行职责，角色通过运行配置控制。
- 后续接入 CI/CD 时，可直接复用 `mvn package -> docker build` 两阶段流水线。

### 2.6 单程序配置分离说明

当前系统采用“单程序、配置分离”的部署方式：

1. 程序只有一个：`mr-app`
2. 镜像只有一个：`mr-app`
3. `mr-api` / `mr-worker` 不是两套独立程序，而是同一程序在不同配置下承担不同职责

推荐配置口径：

| 运行角色 | 说明 | 关键配置示例 |
|---------|------|-------------|
| API 接入角色 | 接收同步请求、异步提交、结果查询 | `MR_JOB_DISPATCHER_ENABLED=false` |
| 后台执行角色 | 负责异步任务拉取、执行、结果持久化 | `MR_JOB_DISPATCHER_ENABLED=true` |

说明：

- 两种角色均使用同一份 Jar、同一份 Dockerfile。
- 后续如果需要区分“接单节点”和“执行节点”，优先通过环境变量和部署配置完成，不新增第二套 Spring Boot 服务。
- 当前 K8s 目录保持单 Deployment 现状，不要求立即拆成两套 Deployment。

---

## 三、K8s 部署

### 3.1 环境准备（Linux 服务器）

#### 安装 Docker Engine

```bash
# CentOS/RHEL
sudo yum install -y yum-utils
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo yum install -y docker-ce docker-ce-cli containerd.io
sudo systemctl enable docker && sudo systemctl start docker

# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y ca-certificates curl gnupg
curl -fsSL https://get.docker.com | sh
sudo systemctl enable docker && sudo systemctl start docker
```

或使用项目提供的脚本：
```bash
sudo ./k8s/scripts/install-docker.sh
```

#### 安装 K3s（推荐）

```bash
# 在线安装（一行命令完成 K8s 集群搭建）
curl -sfL https://get.k3s.io | sh -s - --docker --write-kubeconfig-mode=644

# 验证集群
kubectl get nodes
```

或使用项目脚本：
```bash
sudo ./k8s/scripts/install-k3s.sh
```

> **K3s vs 标准 K8s：** K3s 是经 CNCF 认证的轻量级 K8s 发行版，单二进制文件，内存占用约 512MB。所有标准 K8s 的 YAML 清单、kubectl 命令均通用，后续可无修改迁移到标准 K8s。

### 3.2 K8s 资源清单

当前基础 K8s 资源使用 `mr-app` 作为 Deployment / Service 名称，命名空间使用 `mr-engine`。

#### 命名空间

```yaml
# k8s/base/namespace.yaml
apiVersion: v1
kind: Namespace
metadata:
  name: mr-engine
```

#### ConfigMap（应用配置）

说明：当前 Engine 已切换为 `enginedb.*` / `engineresultdb.*` 双数据源口径，连接类环境变量以 `ENGINE_*` 为准。  
本节中出现的 `MR_JOB_DB_*` 属于历史兼容写法，建议逐步替换。

关键配置项：

| 环境变量 | 说明 | 默认值 |
|---------|------|--------|
| `MR_SERVER_PORT` | 服务端口 | 8080（容器部署口径） |
| `ENGINE_DB_URL` | Engine 输入/任务库连接 | MySQL（默认） |
| `ENGINE_DB_DRIVER` | Engine 输入/任务库驱动 | com.mysql.cj.jdbc.Driver（默认） |
| `ENGINE_RESULT_DB_URL` | Engine 结果库连接 | Doris MySQL 协议（默认 9030） |
| `ENGINE_RESULT_DB_DRIVER` | Engine 结果库驱动 | com.mysql.cj.jdbc.Driver |
| `MR_JOB_CORE_SIZE` | 计算线程池核心数 | 4 |
| `MR_JOB_MAX_SIZE` | 计算线程池最大数 | 16 |
| `MR_FRTB_BATCH_EXECUTOR_CORE_SIZE` | FRTB 计算核心数 | 4 |
| `MR_SCENARIO_SERVICE_ENABLED` | 是否启用情景模块 | false |
| `MR_SECURITY_ENABLED` | 是否启用 API 安全 | false |

说明：`k8s/base/configmap.yaml` 当前仍存在部分历史 `MR_JOB_DB_*` 变量模板；  
生产部署前请按本节口径替换为 `ENGINE_DB_*` / `ENGINE_RESULT_DB_*`。

#### Secret（敏感配置）

```yaml
# k8s/base/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: mr-engine-secret
  namespace: mr-engine
type: Opaque
stringData:
  ENGINE_DB_USERNAME: "sa"
  ENGINE_DB_PASSWORD: ""
  ENGINE_RESULT_DB_USERNAME: "root"
  ENGINE_RESULT_DB_PASSWORD: ""
  MR_SECURITY_TOKENS: ""
```

> **生产环境安全建议：** 使用 Vault、Sealed Secrets 等外部密钥管理方案替代明文 Secret。

#### Deployment（应用部署）

核心配置：

| 配置项 | 值 | 说明 |
|-------|-----|------|
| 副本数 | 2 | 高可用 |
| CPU 请求/限制 | 500m / 2000m | |
| 内存 请求/限制 | 1Gi / 4Gi | 金融计算需要较大内存 |
| 启动探针 | /healthz（100s 超时） | Spring Boot 启动较慢 |
| 存活探针 | /healthz（30s 间隔） | |
| 就绪探针 | /readyz（10s 间隔） | |
| 滚动更新 | maxSurge=1, maxUnavailable=0 | 零停机发布 |

当前仓库中的 `k8s/base/deployment.yaml` 仍采用单 Deployment 方式部署 `mr-app`。  
如果后续需要区分 API 接入实例与后台执行实例，建议继续使用同一镜像，通过环境变量切换角色，而不是拆分第二套程序。

#### HPA（自动伸缩）

```
最小副本: 2
最大副本: 8
CPU 阈值: 70%
内存阈值: 80%
扩容冷却: 60s
缩容冷却: 300s
```

### 3.3 部署流程

#### 一键部署

```bash
# 开发环境
./k8s/scripts/deploy.sh dev

# 生产环境
./k8s/scripts/deploy.sh prod
```

#### 手动逐步部署

```bash
# 1. 创建命名空间
kubectl apply -f k8s/base/namespace.yaml

# 2. 部署配置和密钥
kubectl apply -f k8s/base/configmap.yaml
kubectl apply -f k8s/base/secret.yaml

# 3. 应用环境覆盖（开发 or 生产）
kubectl apply -f k8s/overlays/dev/configmap.yaml    # 开发环境
# kubectl apply -f k8s/overlays/prod/configmap.yaml  # 生产环境

# 4. 部署应用
kubectl apply -f k8s/base/deployment.yaml
kubectl apply -f k8s/base/service.yaml

# 5. 可选：启用自动伸缩（生产环境）
kubectl apply -f k8s/base/hpa.yaml
```

### 3.4 运维操作

```bash
# 查看 Pod 状态
kubectl get pods -n mr-engine

# 查看日志
kubectl logs -n mr-engine -l app=mr-app -f

# 进入容器调试
kubectl exec -it -n mr-engine <pod-name> -- sh

# 手动扩缩容
kubectl scale deployment mr-app -n mr-engine --replicas=4

# 滚动更新（修改镜像版本）
kubectl set image deployment/mr-app -n mr-engine mr-app=your-registry/mr-app:v1.1.0

# 回滚
kubectl rollout undo deployment/mr-app -n mr-engine

# 查看部署历史
kubectl rollout history deployment/mr-app -n mr-engine
```

---

## 四、环境差异化

| 配置项 | 开发环境 | 生产环境 |
|-------|---------|---------|
| 数据库 | MySQL + Doris | Doris（MySQL 协议） |
| 线程池 | 2-4 核 | 8-32 核 |
| 副本数 | 1-2 | 2-8（HPA） |
| 部署角色 | 单实例混合角色 | 可按配置区分 API / Worker 职责 |
| 安全模块 | 关闭 | 开启 |
| 审计告警 | 关闭 | 开启 |
| 情景模块 | 关闭 | 按需开启 |

环境覆盖通过 `k8s/overlays/dev/` 和 `k8s/overlays/prod/` 目录下的 ConfigMap 实现，只需覆盖差异项。

---

## 五、完整部署 Checklist

- [ ] Linux 服务器就绪（CentOS 7+ / Ubuntu 20.04+）
- [ ] 安装 Docker Engine
- [ ] 安装 K3s 或标准 K8s
- [ ] 项目代码传输到服务器（含 engine/、docker/ 和 k8s/ 目录）
- [ ] 构建 Docker 镜像
- [ ] 修改 Secret 中的数据库凭证
- [ ] 根据环境选择 dev/prod 配置
- [ ] 执行部署脚本
- [ ] 验证 Pod 状态和健康检查
- [ ] 验证 API 接口可用性

---

## 六、Linux 多机器集群部署

本章节说明如何在多台 Linux 服务器上部署市场风险系统集群。基于现有代码**无需改造程序结构**，主要调整部署配置与资源参数。

### 6.1 集群架构

```
┌─────────────────────────────────────────────────────────┐
│                   负载均衡 / Ingress                      │
└───────────────────────┬─────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
   ┌─────────┐    ┌─────────┐    ┌─────────┐
   │ 节点 1   │    │ 节点 2   │    │ 节点 3   │
   │ (主节点) │    │ (工作节点)│   │ (工作节点)│
   │ K3s     │    │ K3s     │    │ K3s     │
   │ Server  │    │ Agent   │    │ Agent   │
   │         │    │         │    │         │
   │ Pod×2   │    │ Pod×2   │    │ Pod×2   │
   └─────────┘    └─────────┘    └─────────┘
        │               │               │
        └───────────────┼───────────────┘
                        ▼
               ┌────────────────┐
               │  外部数据库     │
               │  Doris / MySQL │
               └────────────────┘
```

### 6.2 服务器要求

| 角色 | 最低配置 | 推荐配置 | 数量 |
|------|---------|---------|------|
| 主节点（K3s Server） | 2C4G | 4C8G | 1 |
| 工作节点（K3s Agent） | 4C8G | 8C16G | 2+ |
| 数据库（Doris/MySQL） | 4C8G | 8C32G | 独立部署 |

**操作系统要求：** CentOS 7+ / Ubuntu 20.04+ / Rocky Linux 8+

### 6.3 集群搭建步骤

#### 第一步：所有节点安装 Docker

在**每台服务器**上执行：

```bash
# 传输安装脚本
scp k8s/scripts/install-docker.sh user@节点IP:/tmp/
ssh user@节点IP "sudo chmod +x /tmp/install-docker.sh && sudo /tmp/install-docker.sh"
```

#### 第二步：主节点安装 K3s Server

```bash
# 在主节点上执行
curl -sfL https://get.k3s.io | sh -s - \
    --docker \
    --write-kubeconfig-mode=644 \
    --tls-san=主节点公网IP \
    --node-name=mr-master

# 验证主节点就绪
kubectl get nodes
#  NAME        STATUS   ROLES                  AGE   VERSION
#  mr-master   Ready    control-plane,master   30s   v1.29.x+k3s1

# 获取工作节点加入令牌
cat /var/lib/rancher/k3s/server/node-token
```

#### 第三步：工作节点加入集群

在**每台工作节点**上执行：

```bash
# 替换 主节点IP 和 令牌
curl -sfL https://get.k3s.io | \
    K3S_URL=https://主节点IP:6443 \
    K3S_TOKEN=令牌值 \
    sh -s - \
    --docker \
    --node-name=mr-worker-N

# N 为节点编号：mr-worker-1, mr-worker-2, ...
```

#### 第四步：验证集群

回到主节点执行：

```bash
kubectl get nodes
#  NAME          STATUS   ROLES                  AGE   VERSION
#  mr-master     Ready    control-plane,master   5m    v1.29.x
#  mr-worker-1   Ready    <none>                 2m    v1.29.x
#  mr-worker-2   Ready    <none>                 1m    v1.29.x
```

### 6.4 多集群配置调整

相比单机部署，多集群需要调整以下 K8s 配置：

#### 调整一：数据库使用外部共享数据库（必须）

多 Pod 分布在不同节点，必须使用外部共享数据库。

修改 `k8s/overlays/prod/configmap.yaml`（已预置）：

```yaml
data:
  ENGINE_DB_URL: "jdbc:mysql://数据库IP:3306/mr_engine?useUnicode=true&characterEncoding=UTF-8&rewriteBatchedStatements=true"
  ENGINE_DB_DRIVER: "com.mysql.cj.jdbc.Driver"
  ENGINE_RESULT_DB_URL: "jdbc:mysql://数据库IP:9030/engine_result_db?useUnicode=true&characterEncoding=UTF-8&rewriteBatchedStatements=true"
```

修改 `k8s/base/secret.yaml`：

```yaml
stringData:
  ENGINE_DB_USERNAME: "实际用户名"
  ENGINE_DB_PASSWORD: "实际密码"
  ENGINE_RESULT_DB_USERNAME: "实际用户名"
  ENGINE_RESULT_DB_PASSWORD: "实际密码"
```

#### 调整二：节点 ID 自动化（推荐）

使 K8s 自动注入 Pod 名称作为节点 ID，便于追踪任务分配。

修改 `k8s/base/deployment.yaml`，在 `env` 段增加：

```yaml
env:
  - name: MR_JOB_NODE_ID
    valueFrom:
      fieldRef:
        fieldPath: metadata.name
  # 保留原有 JAVA_OPTS ...
```

#### 调整三：副本数与资源（按节点数调整）

修改 `k8s/base/deployment.yaml`：

```yaml
spec:
  replicas: 6    # 3 节点 × 每节点 2 Pod

  # 资源限制根据节点配置调整
  resources:
    requests:
      cpu: "1000m"      # 提高到 1 核
      memory: "2Gi"     # 提高到 2G
    limits:
      cpu: "4000m"      # 最大 4 核
      memory: "8Gi"     # 最大 8G
```

修改 `k8s/base/hpa.yaml`：

```yaml
spec:
  minReplicas: 3     # 至少每节点 1 个 Pod
  maxReplicas: 18    # 最大每节点 6 个 Pod
```

#### 调整四：生产线程池（按资源放大）

修改 `k8s/overlays/prod/configmap.yaml`：

```yaml
data:
  MR_JOB_CORE_SIZE: "8"
  MR_JOB_MAX_SIZE: "32"
  MR_JOB_QUEUE_CAPACITY: "2000"
  MR_FRTB_BATCH_EXECUTOR_CORE_SIZE: "8"
  MR_FRTB_BATCH_EXECUTOR_MAX_SIZE: "16"
```

#### 调整五：按配置区分 API / Worker 角色（可选）

如果后续希望在集群中让一部分实例偏向 API 接入、另一部分实例偏向后台执行，推荐继续使用同一镜像，通过环境变量区分职责：

```yaml
data:
  MR_JOB_DISPATCHER_ENABLED: "true"
```

建议口径：

- API 角色：关闭 dispatcher，只处理接口接入、任务提交和结果查询。
- Worker 角色：开启 dispatcher，负责轮询任务和执行计算。
- 两者基于同一套 `mr-app` 程序，不新增第二套 Spring Boot 服务。

#### 调整六：主控机 / Pricing Worker 资源分配建议（推荐）

当前系统推荐按以下方式分配资源职责：

- 主控机：
  - 承担批次总编排入口，例如 `/api/v1/jobs/batch/run`
  - 执行情景生成
  - 执行 FRTB 敏感性计量 / FRTB 汇总
  - 也可以运行 `pricing` / `scenario` 批量任务，但不作为主要算力节点
- Pricing Worker：
  - 主要承担 `PRICING` / `SCENARIO` 的批量任务执行
  - 不作为批次总编排入口暴露
  - 作为主要算力池横向扩展

推荐原则：

- 情景生成与 FRTB 相关计算集中在主控机，便于控制树级汇总语义和批次执行顺序。
- `pricing` 与情景损益批量计量分发到多台 Worker，提高吞吐。
- 主控机允许执行 `pricing`，但并发配置应明显小于 Worker，避免抢占控制面资源。
- 第一阶段优先通过部署拓扑和配置实现，不要求程序内新增 `node_role`。

建议配置差异如下：

| 角色 | 对外入口 | `MR_JOB_DISPATCHER_ENABLED` | pricing 并发 | FRTB 线程池 | 说明 |
|------|----------|-----------------------------|--------------|-------------|------|
| 主控机 | 暴露 | `true` | 小 | 正常/偏高 | 负责批次编排、情景生成、FRTB 汇总，也可兜底执行少量 pricing |
| Pricing Worker | 可不暴露 | `true` | 大 | 可较小 | 主要消费批量 `PRICING` / `SCENARIO` 任务 |

主控机建议：

- 保留完整服务能力，确保：
  - 情景生成
  - FRTB 敏感性计量 / 汇总
  - BatchRun 编排
  均可在本机完成
- `pricing` 线程池配置取较小值，例如：
  - `MR_JOB_CORE_SIZE=2`
  - `MR_JOB_MAX_SIZE=8`

Pricing Worker 建议：

- 继续使用同一镜像与同一程序
- 主要通过更大的线程池参数承接批量 pricing，例如：
  - `MR_JOB_CORE_SIZE=8`
  - `MR_JOB_MAX_SIZE=32`
- 不必承担批次总编排职责

说明：

- 这种方式属于“单程序、配置分工”，不要求拆分第二套 Spring Boot 服务。
- 如果后续发现主控机经常被 pricing 任务抢满，再考虑增加程序级节点角色控制；当前版本优先保持简单。

### 6.5 多集群部署流程

```bash
# 1. 构建镜像（在任一节点上）
./docker/build-image.sh v1.0.0

# 2. 所有节点加载镜像（无私有仓库时）
docker save mr-app:v1.0.0 -o mr-app-v1.0.0.tar
scp mr-app-v1.0.0.tar user@worker-1:/tmp/
scp mr-app-v1.0.0.tar user@worker-2:/tmp/
ssh user@worker-1 "docker load -i /tmp/mr-app-v1.0.0.tar"
ssh user@worker-2 "docker load -i /tmp/mr-app-v1.0.0.tar"

# 3. 或者使用私有镜像仓库（推荐）
docker tag mr-app:v1.0.0 registry.内网:5000/mr-app:v1.0.0
docker push registry.内网:5000/mr-app:v1.0.0
# 同时修改 deployment.yaml 中 image 为 registry.内网:5000/mr-app:v1.0.0

# 4. 部署应用
./k8s/scripts/deploy.sh prod

# 5. 验证分布
kubectl get pods -n mr-engine -o wide
#  NAME                        READY   STATUS    NODE
#  mr-app-xxx-abc              1/1     Running   mr-worker-1
#  mr-app-xxx-def              1/1     Running   mr-worker-2
#  mr-app-xxx-ghi              1/1     Running   mr-master
```

### 6.6 多集群运维

#### 查看各节点 Pod 分布

```bash
kubectl get pods -n mr-engine -o wide --sort-by=.spec.nodeName
```

#### 查看集群资源使用

```bash
kubectl top nodes
kubectl top pods -n mr-engine
```

#### 节点维护（摘除节点）

```bash
# 标记节点不可调度
kubectl cordon mr-worker-1

# 驱逐节点上的 Pod（会自动迁移到其他节点）
kubectl drain mr-worker-1 --ignore-daemonsets --delete-emptydir-data

# 维护完成后恢复
kubectl uncordon mr-worker-1
```

#### 添加新节点扩容

```bash
# 新节点安装 Docker
sudo ./install-docker.sh

# 加入集群
curl -sfL https://get.k3s.io | \
    K3S_URL=https://主节点IP:6443 \
    K3S_TOKEN=令牌 \
    sh -s - --docker --node-name=mr-worker-N

# K8s 自动调度 Pod 到新节点（HPA 触发时）
```

### 6.7 多集群部署 Checklist

- [ ] 所有节点安装 Docker Engine
- [ ] 主节点安装 K3s Server
- [ ] 工作节点加入集群（获取 node-token）
- [ ] 验证 `kubectl get nodes` 所有节点 Ready
- [ ] 外部数据库就绪（Doris/MySQL）
- [ ] 更新 ConfigMap：数据库连接、线程池参数
- [ ] 更新 Secret：数据库凭证
- [ ] 更新 Deployment：节点 ID 自动注入、副本数、资源限制
- [ ] 构建镜像并分发到所有节点（或推送到私有仓库）
- [ ] 执行 `deploy.sh prod` 部署
- [ ] 验证 Pod 分布在多个节点上
- [ ] 验证 API 接口、任务提交和计算结果

---

## 七、本地开发环境配置

### 7.1 本地系统设置总览

| 项目 | 当前设置 |
|------|----------|
| 操作系统 | Windows（主开发环境） |
| 终端 | PowerShell（命令前设置 UTF-8 输出编码） |
| Linux 运行时 | WSL2（Ubuntu） |
| 容器 | Docker Desktop（启用 WSL Integration） |
| JDK | 17（LTS） |
| zcyh_web 端口 | 8080 |
| engine 端口 | 8081 |

### 7.2 数据存储分层与目标态

当前与目标口径统一如下：

- `zcyh_web` 业务配置/管理数据：MySQL（`ry-vue`）。
- `engine` 计量结果数据：目标态为 Doris，作为最终结果存储。
- `engine` 输入/任务库：MySQL（`engine_db`）。

结果表范围（示例）：

- `TB_OUT_TRADE_RESULT_DETAIL`
- `TB_OUT_TRADE_FRTB_SENSITIVITY_DETAIL`
- `TB_OUT_TRADE_DRC_DETAIL`
- `TB_OUT_TRADE_SCENARIO_RESULT_DETAIL`
- `TB_OUT_TRADE_SCENARIO_VAR_RESULT_DETAIL`
- `TB_OUT_MARKET_DATA_DETAIL`（市场曲线结果表）

市场曲线结果口径（当前实现）：

- 同步链路接口返回中的 `generated_market_data` 是本次曲线生成的全量原始输出。
- 批量链路落库到 `TB_OUT_MARKET_DATA_DETAIL` 的是合并后结果：
  - 先保留外部 `market_data`；
  - 再补入外部缺失的 `generated_market_data`；
  - 冲突键（`CURVE_TYPE + CURVE_ID/FIXING_ID`）始终外部优先，不覆盖。

迁移原则（文档口径）：

1. 先完成 Doris 表结构与写入链路验证。
2. 再切换 engine 结果写入默认目标为 Doris。
3. 结果输出统一由 Doris 承接。

### 7.3 zcyh_web 后端数据库连接（MySQL）

配置文件：`E:\zcyh_mr\zcyh_web\zcyh-admin\src\main\resources\application-druid.yml`

| 参数 | 值 |
|------|------|
| 数据库类型 | MySQL 8.0.28 |
| 安装路径 | E:\mysql |
| JDBC URL | `jdbc:mysql://127.0.0.1:3306/ry-vue?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&sslMode=DISABLED&serverTimezone=GMT%2B8&allowMultiQueries=true&allowPublicKeyRetrieval=true` |
| 用户名 | root |
| 密码 | （空） |
| 数据库名 | ry-vue |

> 注意：JDK 17 与 MySQL 8.0.28 存在 TLS 兼容性问题，JDBC URL 中必须包含 `sslMode=DISABLED` 和 `allowPublicKeyRetrieval=true`，否则连接可能报 `Connection reset`。

### 7.4 Doris 本地联调与连接方式（engine 结果库目标态）

运行方式（当前已验证）：

- Windows + WSL2(Ubuntu) + Docker Desktop
- 容器名：`jie-fe-1`、`jie-be-1`
- Docker 网络：`doris-net`（bridge）
- 固定 IP：FE=`172.20.80.2`，BE=`172.20.80.3`

端口映射：

| 服务 | 容器端口 | 主机访问地址 |
|------|----------|--------------|
| FE HTTP | 8030 | `http://127.0.0.1:8030` |
| FE MySQL 协议 | 9030 | `127.0.0.1:9030` |
| FE EditLog | 9010 | `127.0.0.1:9010` |
| BE HTTP | 8040 | `http://127.0.0.1:8040` |
| BE Heartbeat | 9050 | `127.0.0.1:9050` |

推荐启动命令（在 Ubuntu 中执行）：

```bash
docker network create --subnet 172.20.80.0/24 doris-net

docker run -d --name jie-fe-1 --hostname fe \
  --network doris-net --ip 172.20.80.2 \
  -p 8030:8030 -p 9030:9030 -p 9010:9010 \
  -e FE_SERVERS=fe1:172.20.80.2:9010 \
  -e FE_ID=1 \
  apache/doris:fe-4.0.1

docker run -d --name jie-be-1 --hostname be \
  --network doris-net --ip 172.20.80.3 \
  -p 8040:8040 -p 9050:9050 \
  -e FE_SERVERS=fe1:172.20.80.2:9010 \
  -e BE_ADDR=172.20.80.3:9050 \
  apache/doris:be-4.0.1
```

连接与健康检查：

```bash
# 在 Ubuntu 中
docker exec jie-fe-1 mysql -h127.0.0.1 -P9030 -uroot -e "show backends"
docker exec jie-fe-1 mysql -h127.0.0.1 -P9030 -uroot -e "select 1 as ok"
```

```powershell
# 在 Windows PowerShell 中
Test-NetConnection 127.0.0.1 -Port 9030
Test-NetConnection 127.0.0.1 -Port 8030
```

说明：

- 在 WSL2 + Docker Desktop 场景，推荐 `bridge + 端口映射`。
- 不建议使用 `host` 网络作为本机联调默认方案，以避免“容器内可用、宿主机不可达”的问题。

### 7.5 Redis

| 参数 | 值 |
|------|------|
| 运行方式 | Docker 容器（当前） |
| 容器名 | mr-redis |
| 镜像 | redis:alpine |
| 端口 | 6379（默认） |
| 密码 | （空） |

启动命令：

```powershell
docker start mr-redis
```

> 兼容说明：若切回本地 Windows Redis，也可使用 `E:\Redis\redis-server.exe E:\Redis\redis.windows.conf`。  
> 后端配置文件 `application.yml` 中 Redis 地址保持 `localhost` 即可。

### 7.6 前端配置与登录

配置文件：`E:\zcyh_mr\zcyh-ui-v3\.env.development`

| 参数 | 值 | 说明 |
|------|------|------|
| VITE_APP_MOCK | false | 关闭 Mock 模式，使用真实后端接口 |
| VITE_DEV_BYPASS_AUTH | true | 开发态绕过登录，自动登录后端获取 token |
| VITE_APP_BASE_API | /dev-api | 后端接口代理前缀 |

Vite 代理配置将 `/dev-api` 转发到 `http://localhost:8080`。

系统登录账号：

| 用户名 | 密码 | 说明 |
|------|------|------|
| admin | admin123 | 管理员账号（BCrypt 加密存储） |

> 开启 `VITE_DEV_BYPASS_AUTH=true` 后，前端会自动使用 admin/admin123 调用后端 `/login` 接口获取 JWT token，无需手动登录。

### 7.7 本地启动顺序（统一口径）

1. 启动 Docker Desktop，确认 WSL Integration 已开启。
2. 启动 Doris（FE/BE），并执行 `show backends` 确认 BE `Alive=true`。
3. 启动 MySQL（3306）。
4. 启动 Redis（当前为 Docker 容器 `mr-redis`，端口 6379）。
5. 启动 `zcyh_web`（fat jar，禁止 `mvn spring-boot:run`）：

```powershell
cd E:\zcyh_mr\zcyh_web
mvn package -pl zcyh-admin -am -q "-DskipTests"
java -jar zcyh-admin\target\zcyh-admin.jar
```

6. 启动 `engine`（fat jar）：

```powershell
cd E:\zcyh_mr\engine
mvn package -pl mr-app -am -q "-DskipTests"
java -jar mr-app\target\mr-app-1.0-SNAPSHOT.jar
```

7. 启动前端：

```powershell
cd E:\zcyh_mr\zcyh-ui-v3
npm run dev
```

访问地址：`http://localhost:5173/`

### 7.8 输入库 MySQL 部署说明（排除 calendar）

迁移策略（推荐）：

1. 使用独立 schema：`mr_engine`（engine）与 `ry-vue`（web）分离。
2. 账号策略：迁移与运行统一使用 `engine_app`；web 复用现有业务账号并按需授予 `mr_engine` 访问权限。
3. 不依赖“仅靠表名前缀”做隔离，避免权限边界不清晰。

迁移范围（当前口径）：

- `MR_TRADE_INPUT`
- `MR_MARKET_CURVE_INPUT`
- `MR_RISKFACTOR_DATA`
- `MR_SCENARIO_RULE`
- `MR_AGG_RULE`
- `MR_ASYNC_JOB`
- `MR_ASYNC_BATCH_JOB`
- `MR_ASYNC_BATCH_ITEM`
- `MR_AUDIT_LOG`

不在本次迁移范围：

- `calendar`（已改为缓存统一处理）
- `TB_OUT_*`（输出结果由 Doris 承接）

脚本目录：请使用当前 MySQL 初始化脚本（按运维仓库标准路径维护）。
